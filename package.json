{
  "name": "appcoticuenta",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd frontend && npx serve -s dist -l ${PORT:-8080}"
  }
}
